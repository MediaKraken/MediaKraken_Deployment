# To startup the application
./mediakraken_start.sh

# To shutdown the application
./mediakraken_stop.sh

# To update the application
python mediakraken_update_images.py
./mediakraken_restart.sh


Access the HOST machine via a web browser at HOST:8900 address.

Register a user via the link at top right of the screen.

The first user to be registered will automatically be an administrator.
